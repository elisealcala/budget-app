generator client {
  provider = "prisma-client-js"
}

model User {
  id  String @id @default(uuid())
  username String @unique
  password String
  salt String
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

